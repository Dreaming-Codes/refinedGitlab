export const betterChangeCountsMeta = {
  id: 'better-change-counts',
  name: 'Better change line counts',
  description:
    'On merge requests, show file and line totals that exclude lockfiles and generated files.',
  defaultEnabled: true,
} as const;
